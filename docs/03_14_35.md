# 다른 작업과 동시 편집 충돌 — 조사 보고 (2026-09-03 14:35)

작업 중 예상 못 한 상태를 발견해 **편집을 멈추고 조사만 했다.** 조치는 승인 후.

---

## 1. 무슨 일이 있었나

**이 저장소를 나 말고 다른 무언가가 지금도 고치고 있다.**

| 시각 | 사실 |
|---|---|
| 12:59 | `src/assets/images/design-mock-01~04.svg` 4개 생성 — **내가 만들지 않았다** |
| 12:59 | `portfolio.js` 의 `DESIGN.cards` 가 그 목업 SVG 를 쓰도록 변경 — 내가 하지 않았다 |
| (시각 미상) | 커밋 `c66467b` "feat: MY PROJECTS 카드 및 스크롤 연출 버튼에 외부 링크 연결" — **내가 만들지 않은 커밋** |
| 14:31:57 | `portfolio.js` 수정됨 (목업 SVG 참조가 다시 **제거**됨) |
| 14:32:45 | `MyDesign.jsx` 수정됨 |
| 14:33 | `src/assets/images/Mockup1.svg` 새로 생김 (미추적) |

즉 **12:59 에 넣은 목업을 14:31 에 다시 빼고 다른 목업으로 교체하는 중**으로 보인다.

## 2. 내 작업은 어떻게 됐나

커밋 `c66467b` 가 **내 미커밋 작업까지 함께 쓸어 담아 커밋했다.**
그 커밋에 들어간 내 파일:

- `docs/03_12_12.md`, `docs/03_12_50.md`, `docs/03_12_53.md` (보고서 3건)
- `portfolio.js` 의 `notice` 2건(IKEA · YouTube Music 컨셉 고지)
- `portfolio.js` 의 `cardTitle: ""` / `cardLines: []` (프라다 인용문 삭제)

**데이터는 살아남았다(확인함).** 다만 커밋 메시지는 외부 링크 작업 내용이라
내 변경 이력이 그 안에 섞여 들어갔다.

## 3. 현재 상태 — 무엇이 되고 무엇이 안 되나

| 항목 | 상태 |
|---|---|
| 프라다 인용문 삭제 | **완료** — `cardTitle: ""`, `cardLines: []` |
| MY DESIGN 오버레이 숨김 처리 | **완료** — `MyDesign.jsx:192` 에 빈 값 가드가 들어가 있다 |
| IKEA · YouTube Music 고지 **데이터** | **완료** — `portfolio.js` 에 `notice` 2건 |
| IKEA · YouTube Music 고지 **화면 표시** | ❌ **안 됨.** `notice` 를 그리는 코드가 없다 |
| `LICENSE-THIRD-PARTY.md` | **완료** — 방금 새로 만들었다 (충돌 없는 새 파일) |
| 푸터 크레딧 한 줄 | ❌ **보류** |
| `ai-cli` 에 Blender 로고 오류 | ❌ 미처리 (아이콘을 정해야 함) |

## 4. 왜 멈췄나

남은 두 가지를 하려면 **지금 다른 작업이 고치고 있는 바로 그 파일들**을 건드려야 한다.

- 고지 화면 표시 → `ProjectCard.jsx`, `Projects.jsx`, `MobileProjects.jsx`
  (셋 다 `c66467b` 에서 방금 수정됐다)
- 푸터 크레딧 → `portfolio.js` (14:31 에 수정됨, 지금도 작업 중으로 보임)

**둘이 같은 파일을 동시에 쓰면 서로의 작업을 덮어쓴다.**
내가 지금 저장하면 상대의 진행 중인 변경이 날아갈 수 있다.

## 5. 확인 부탁

1. **다른 창/다른 도구로 이 저장소를 함께 작업 중인가?** (agy·Codex 등)
   - 그렇다면 그쪽이 끝난 뒤에 알려주면 이어서 하겠다
   - 아니라면 원인을 더 찾아봐야 한다 — **현재로선 확인 안 됨**
2. `design-mock-01~04.svg` 와 `Mockup1.svg` 는 내가 만든 게 아니다. 그대로 둘까?
3. `ai-cli` 항목의 Blender 로고는 무엇으로 바꿀까?
   (`illustrator` 처럼 `markLabel` 로 글자만 넣는 방법이 있다 — 예: `>_`)
