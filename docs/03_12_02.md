# ABOUT ME 사진 교체 — 결과 (2026-09-03 12:02)

요청: 피그마 `node-id=20-971` 의 ABOUT ME 사진(밝기 보정본)으로, **사이트 사이즈 그대로** 교체.
(원본 확장 = 앞선 보고서의 선택지 B 는 하지 않음)

---

## 1. 무엇을 바꿨나

`src/assets/images/about-portrait.webp` **파일 하나만** 교체했다. 코드 변경 없음.

| 항목 | 이전 | 이후 |
|---|---|---|
| 출처 | 보정 전 원본 | 피그마 `120:55` "iPad Pro Upper Body v3 1" 내보내기 |
| 크기 | 869 × 551 | **869 × 551 (동일)** |
| 용량 | 11,214 B | 11,642 B (+428 B) |
| 배경 | `#f8f8f8` | `#f8f8f8` (동일) |
| 인코딩 | WebP 손실 | WebP 손실 q86 |

크기·배경색·파일명·경로가 모두 같으므로 `AboutMe.jsx` / `MobileAbout.jsx` /
`portfolio.js` 는 손대지 않았다.

---

## 2. 실제로 무엇이 달라졌나 — 밝기 보정

평균 RGB 를 영역별로 재봤다.

| 영역 | 이전 (보정 전) | 이후 (피그마 보정본) |
|---|---|---|
| 얼굴 부근 | (169.6, **152.8**, **143.3**) | (171.2, **162.7**, **161.2**) |
| 옷 · 몸통 | (186.3, **174.4**, **169.5**) | (189.3, **188.4**, **192.8**) |
| 전체 | (230.2, 226.3, 224.4) | (230.8, 229.3, 229.5) |

이전 파일은 **초록·파랑 채널이 낮아 노란 기가 돌았다.** 특히 옷에서 차이가 크다
(파랑 169.5 → 192.8). 보정본은 화이트밸런스가 잡혀 **흰 셔츠가 실제로 희게** 나온다.

---

## 3. 검증

| 확인 | 결과 |
|---|---|
| 피그마 원본 대비 화질 손실 | **PSNR 45.8 dB** — 육안 무손실 수준 |
| 파일 형식 | `RIFF/WEBP/VP8 ` 정상 |
| 해상도 | 869 × 551 — 시안 프레임과 일치 |
| 귀퉁이 배경 픽셀 | `(248, 248, 248)` = `#f8f8f8` — 페이지 배경과 일치 |
| `npm run build` | **성공** (1.34s) — `dist/assets/about-portrait-CO1fUNgS.webp` 11.64 kB |
| JS 번들 | 382.28 kB — 변동 없음 |

이전 파일은 `_scratchpad/about-portrait.backup.webp` 에 백업해뒀다(세션 임시 폴더라
영구 보관은 아니다 — 되돌릴 일이 있으면 `git checkout` 이 확실하다).

---

## 4. 남은 것

- 커밋은 아직 하지 않았다. 화면에서 확인 후 말해주면 커밋하겠다.
- 앞선 보고서 `docs/03_11_56.md` 의 "같은 사진이다" 결론은 틀렸고, 해당 파일 상단에 정정을 달았다.
