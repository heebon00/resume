# 빌드 실패 보고 — logo-brand.svg 없음

작성 2026-09-02 16:34 · 상태: **조사만 함, 아무것도 고치지 않음**

## 무슨 일이 있었나

모바일 연출 섹션 위치를 옮긴 뒤 `npm run build` 를 돌렸더니 빌드가 실패했다.

```
[UNRESOLVED_IMPORT] Could not resolve '../assets/icons/logo-brand.svg'
  in src/components/MobileHeader.jsx:2
```

`npm run lint` 는 통과한다(oxlint 는 에셋 경로를 보지 않는다).

## 현재 상태

- `src/components/MobileHeader.jsx:1` 이 `../assets/icons/logo-brand.svg` 를 import 한다 (유일한 사용처)
- `src/assets/icons/` 에 **`logo-brand.svg` 파일이 없다**. 남아 있는 파일은 8개:
  cursor-arrow-a/b, decor-arrow, donut-7, icon-arrow-left, icon-arrow-right,
  icon-arrow-right-sm, pattern-footer-lines
- `pattern-line.svg` 도 함께 없어졌다. 이쪽은 쓰던 `BackgroundPattern` 이
  `App.jsx` 에서 이미 빠져 있어 빌드에 영향을 주지 않는다.
- 폴더 수정 시각: **오늘 16:33** (다른 파일들은 어제 15:09~17:21)

## 영향 범위

- `npm run build` 실패 → **배포 불가**
- 개발 서버(`npm run dev`)에서도 해당 모듈을 불러올 때 오류가 난다 →
  모바일 헤더가 있는 화면(1280 미만)이 정상적으로 안 뜰 수 있다
- **데이터 손실 여부**: 소스 코드 손실은 없다. 잃은 것은 이미지 에셋 파일 2개다.
  이 저장소는 git 저장소가 아니라(`git init` 안 됨) 파일 복원 이력이 없다.

## 추정 원인 (확정 아님)

이번 작업 중에 `Projects.jsx` · `ProjectCard.jsx` · `App.jsx` · `index.css` 가
내 편집과 별개로 여러 번 바뀌었다(툴이 "changed on disk" 로 알려 줌).
`BackgroundPattern` 이 `App.jsx` 에서 빠진 것도 그 변경들 중 하나다.
같은 작업 흐름에서 **더 이상 안 쓰는 아이콘을 정리하다가 `logo-brand.svg` 까지
지웠는데 `MobileHeader.jsx` 의 import 는 남아 있는 상태**로 보인다.
다만 누가 어떤 의도로 지웠는지는 **확인 안 됨** — 진행 중인 작업일 수도 있다.

## 선택지별 조치안

1. **그대로 둔다** — 다른 작업이 진행 중이라면 곧 `MobileHeader` 쪽도 정리될 수 있다.
   그동안 빌드는 계속 실패한다.
2. **import 를 걷어낸다** — `MobileHeader.jsx` 에서 로고 import 와 사용부를 제거한다.
   빌드는 살아나지만 모바일 헤더에서 브랜드 로고가 사라진다(디자인과 달라짐).
3. **에셋을 되살린다** — 피그마(20-186)에서 로고 노드를 다시 내려받아
   `src/assets/icons/logo-brand.svg` 로 복원한다. 원래 화면 그대로 돌아온다.
   피그마 노드 확인이 필요하다.

## 확인이 필요한 것

- `logo-brand.svg` 삭제가 **의도한 것인지**, 실수인지
- 의도한 것이라면 모바일 헤더의 로고 자리를 어떻게 할지(제거 / 다른 에셋 / 텍스트)

---

## 조치 결과 (승인 후, 16:35)

**3번(에셋 복원)으로 처리했다.** 디자인을 그대로 유지하는 쪽이다.

- 피그마 `TY7Dc9eSZNeWVZqdj50YYR` 노드 **20:2494** 에서 SVG 를 다시 내려받아
  `src/assets/icons/logo-brand.svg` 로 복원 (943 B)
- 내려받은 SVG 크기 **49.3281 x 22.2574** — `MobileHeader.jsx` 주석의
  시안 값(49.324 x 22.254)과 일치한다. 같은 에셋이 맞다.
- 코드는 한 줄도 고치지 않았다(import·사용부 그대로).

**검증**: `npm run build` 통과 / `npm run lint` 통과 (2026-09-02 16:35)

**추가 확인** — `pattern-line.svg` 는 복원하지 않았다.
쓰던 `src/sections/BackgroundPattern.jsx` 자체가 함께 삭제돼 있어
참조가 남아 있지 않다(의도된 정리로 보인다). 이쪽은 빌드에 영향 없다.
