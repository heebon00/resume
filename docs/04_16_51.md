# MY WORK EXPERIENCE 5번째 행 좌표 없음 (조사 보고)

작성 시각: 2026-09-04 16:51 · 조치 전 보고, **아직 아무것도 고치지 않음**

## 무슨 일이 있었나

MY Design 캐로셀 작업을 끝내고 `git status` 를 확인하다가, 내가 손대지 않은
`src/content/portfolio.js` 가 수정돼 있는 것을 발견했다. 내용은 EXPERIENCE 경력
연도 표기 변경(`09` → `2009` 등)과 **5번째 행 추가**다.

```
+ { id: "exp-5", index: "", title: "메가스터디 잇츠 리얼타임  스터디카페 (선임 서포터즈", year: "2023~2025" },
```

## 현재 상태

- 데이터는 5행, 화면 좌표는 4행분만 있다.
  `src/sections/Experience.jsx:18` → `const ROW_TOPS = [164, 248, 319, 402];`
- 5번째 행은 `ROW_TOPS[4]` 가 `undefined` 라서
  `style={{ top: "calc(undefined * var(--u))" }}` 가 되고, 브라우저가 이 선언을
  버린다. 결과적으로 `top` 이 지정되지 않은 절대배치 요소가 되어 컨테이너 위쪽
  (제목·라벨 바 근처)에 겹쳐 그려진다. — 코드 기준 판단이며 화면 확인은 아직 안 했다.
- 모바일(`MobileExperience.jsx`)은 세로로 흘리는 목록이라 영향 없을 가능성이 높다(미확인).
- 데이터 손실은 없다. 커밋되지 않은 작업 트리 변경 상태다.

## 영향 범위

- 데스크톱 MY WORK EXPERIENCE 섹션 레이아웃만.
- 배포본에는 아직 없다(푸시 전 로컬 변경).

## 추정 원인 (확정 아님)

`Experience.jsx` 는 시안 20:1118~20:1184 의 4행 좌표를 하드코딩해 두었고,
행 개수가 늘어나는 경우를 상정하지 않았다. 데이터만 5행으로 늘리면 어긋난다.

## 선택지별 조치안

1. **5행 간격으로 재배치 (권장)**
   컨테이너 높이 557 · 마감 실선 top 527 안에 5행이 들어가도록 `ROW_TOPS` 를
   다시 계산한다(예: 150 / 225 / 300 / 375 / 450). 시안 대비 행 간격이 조금 좁아진다.
2. **컨테이너를 늘려서 5행 배치**
   섹션 높이와 마감 실선 top 을 함께 키운다. 아래 섹션들이 절대좌표라
   전체 캔버스 좌표를 밀어야 할 수 있어 영향 범위가 크다.
3. **행 개수에 맞춰 자동 계산**
   `ROW_TOPS` 하드코딩을 버리고 시작 top + 간격으로 계산한다. 앞으로 행이 늘어도
   안 깨지지만 시안 좌표와 완전히 같지는 않게 된다.

## 확인할 것

- 5번째 행 제목의 여는 괄호가 닫히지 않았다: `(선임 서포터즈` → `(선임 서포터즈)` 가 맞는지.
- 제목 안에 공백이 두 칸 들어가 있다(`리얼타임  스터디카페`). 의도인지.
- 위 1~3 중 어느 방식으로 갈지.

---

## 조치 결과 (2026-09-04, 사용자 승인 — "4번째처럼 5번째도 똑같이")

위 선택지 중 **2번(컨테이너를 늘려 5행 배치)** 으로 진행했다. 행 간격·스타일은
기존 4행과 동일하게 두고 자리만 하나 더 만들었다.

- `src/sections/Experience.jsx`
  - `ROW_TOPS` 에 `485` 추가 (4행 간격 83 을 그대로 이음)
  - 컨테이너 높이 `h-557` → `h-640`
  - 마감 실선 `top-527` → `top-610`
- 아래 MY PROJECTS 섹션은 캔버스 2749 에서 시작하므로 겹치지 않는다
  (경력 섹션 끝 1968 + 640 = 2608).
- 모바일(`MobileExperience.jsx`)은 세로 목록이라 코드 변경 없이 5행이 그대로 붙는다.
- 데스크톱·모바일 헤드리스 스크린샷으로 확인 완료.

### 남은 확인 사항
- 5번째 행 제목의 여는 괄호가 아직 닫히지 않았다: `(선임 서포터즈` (내용이라 손대지 않음).
